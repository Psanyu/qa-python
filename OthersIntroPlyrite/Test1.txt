Issues fixed:

Selector syntax — Classes need dot notation (.class-name), not spaces:
❌ #block14 ui-draggable
✅ #block14.ui-draggable
Multiple classes — Chain them with dots:
❌ ol#field14 ui-droppable ui-sortable
✅ ol#field14.ui-droppable.ui-sortable
Hard waits — Reduced unnecessary timeout. Playwright's drag_to() already handles the drag action completion.
Removed page.close() — Don't close the page inside the test; let your test framework's fixtures handle cleanup.